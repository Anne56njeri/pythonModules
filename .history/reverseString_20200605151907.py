# looping through the array 
# brute method is to switch the positions of the strings using 
# there index's 
# ofcourse should move to h should move to o and vice versa 
# so at any point 0--> length - 1, 1---> le

def reverse(str):


reverse(["h","e","l","l","o"])